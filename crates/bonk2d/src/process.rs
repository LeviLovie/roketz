//! Defines the `process` function to process all objects with a collider component.

use anyhow::Result;
use bevy_ecs::prelude::*;

/// Process a BevyECS world to hadle collisions.
pub fn process(_world: &mut World) -> Result<()> {
    Ok(())
}
